package Logica;

import grafica.Pantalla;

public class PruebaCapas {
    public static void main(String[] args) {
        
        Pantalla panta = new Pantalla();  //Creamos el objeto panta
        panta.setVisible(true);   //Hacemos que la pantalla sea visible
        panta.setLocationRelativeTo(null); //Hacemos que la pantalla salga al medio de la pantalla
        
    }
    
}
